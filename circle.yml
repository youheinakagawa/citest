machine:
  php:
    version: 5.6.5
dependencies:
  override:
    - composer install --prefer-source --no-interaction --no-dev
  pre:
  post:
    - http://selenium-release.storage.googleapis.com/2.53/selenium-java-2.53.0.zip
    - unzip selenium-java-2.53.0.zip
    - java -jar selenium-java-2.53.0.jar:
        background: true
    # install chromedriver
    - wget https://chromedriver.storage.googleapis.com/2.20/chromedriver_linux64.zip
    - unzip chromedriver_linux64.zip
    # run chromedriver
    - ./chromedriver:
        background: true
    - cp ~/citest/MY-SITE /etc/apache2/sites-available
    - a2ensite MY-SITE
    - sudo service apache2 restart
test:
  override:
    - phpunit SampleTest

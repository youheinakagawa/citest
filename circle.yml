machine:
  php:
    version: 5.6.5
dependencies:
  pre:
  post:
    - cp ~/citest/MY-SITE /etc/apache2/sites-available
    - a2ensite MY-SITE
    - sudo service apache2 restart
test:
  override:
    - phpunit SampleTest